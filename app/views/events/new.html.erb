<% page_title t('.title') %>

<%= render 'partials/global_nav', fill: true %>
<%= render 'partials/notice' %>
<div class="container">
  <main>
    <section>
      <h1><%= page_title %></h1>
      <%= render 'events/form', event: event %>
    </section>
  </main>
</div>
